[package]
name = "notation_viewer"
version = "0.3.2"
edition = "2021"
description = "Notation Viewer"

[package.metadata.bundle]
identifier = "fun.notation.viewer"
resources = ["assets"]

[features]
native = [
    "notation_bevy/native",
    "notation_bevy/dsl",
]
dev = [
    "notation_bevy/dev",
    "native",
]
inspector = [
    "notation_bevy/inspector",
    "native",
]

[dependencies]
notation_bevy = { version = "0.3.2", path = "../../crates/notation_bevy" }
